# GulfLedger — Canonical Pack (2026-07-23)
## WHAT THIS IS
Every file Claude maintains, at its latest revision. Upload the ROOT files
to the repo root (overwrites same names). `supabase-function-backups/` and
`sql/` are references — functions deploy via the Supabase dashboard only.

## DO NOT DELETE from the repo (not in this pack, site needs them):
gl-design-system.css · gl-icons.svg · supabase.js · invoice-shared.css ·
schema.sql · api/ · .github/ · supabase/ · *.md docs

## SAFE TO DELETE (verified zero inbound links):
clients.html · customers.html · simplified-invoice.html ·
standard-invoice.html · payroll.html
